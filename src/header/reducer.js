const initialState = {
  foreground: 'black',
  background: 'white',
}

const theme = (state = initialState, action) => {
  switch (action.type) {
    default:
      break
  }

  return state
}

export default theme
